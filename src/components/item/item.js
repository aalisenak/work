import React from 'react';

const Item = () => {


        return (

            <div>
                <button onClick={() => console.log(changeName)}>CLICK </button>
                <p>{count}</p>
            </div>

        );


}

export default Item
